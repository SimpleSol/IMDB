<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.leon.imdb"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".AppDelegate"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.MovieActivity">
            android:parentActivityName=".activity.MainActivity"
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.leon.imdb.activity.MainActivity"/>
        </activity>

        <activity android:name=".activity.MyMoviesActivity">

            android:parentActivityName=".activity.MainActivity"
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.leon.imdb.activity.MainActivity"/>
        </activity>

        <provider
            android:name=".sqlite.SQLiteProvider"
            android:authorities="${applicationId}"
            android:exported="false"/>

    </application>

</manifest>
